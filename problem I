#include <iostream>
#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
    ios::sync_with_stdio(false),cin.tie(nullptr),cout.tie(nullptr);
int n,m;
cin>>n>>m;
map<string,string>map;
    string x,y,s;

    for (int i = 0; i < m; ++i) {
        cin>>x>>y;
        map.emplace(x,y);
    }

    for (int i = 0; i < n; ++i) {
        cin>>s;

        if (map[s].size()>=s.size()){
            cout<<s<<" ";
        }
        else{
            cout<<map[s]<<" ";
        }
    }
    return 0;
}
