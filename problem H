#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false),cin.tie(nullptr),cout.tie(nullptr);
    map<string,string>m;
  string s;
    while (getline(cin,s)){
        if(s.empty())
            break;
        stringstream w (s);
        string x,y;
        w>>x>>y;
        m[y]=x;
    }
    while (cin>>s){
        if (m[s]!="") cout<<m[s]<<"\n";
        else cout<<"eh"<<"\n";
    }
    return 0;
}
